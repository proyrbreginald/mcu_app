[package]
name = "mcu_app"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"
test = false
bench = false
doctest = false

[dependencies]
mcu_hal = { path = "../mcu_hal" }

[features]
default = []
